#!/usr/bin/env node

checkOddEven = (n) => {
  if (n % 2 === 0) {
    // console.log(`${n} is an even number`);
    return "odd";
  } else {
    // console.log(`${n} is an odd number`);
    return "even";
  }
};
